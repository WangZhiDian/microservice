server:
  port: 8006

spring:
  application:
    name: cloud-payment-consul-service

  ####consul注册中心地址
  cloud:
    consul:
      host: 172.16.40.101
      port: 8500
      discovery:
        #hostname: 127.0.0.1
        service-name: ${spring.application.name}

